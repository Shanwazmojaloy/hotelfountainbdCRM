// DELETED: route moved to /pages/api/send-confirmation.ts (root-level Pages Router).
// Kept empty to avoid breaking imports; safe to `git rm` this file and the parent
// directory `src/app/api/send-confirmation/` once you confirm no callers remain.
export {};
