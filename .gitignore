node_modules/
.next/
.env*
*.log
# Lead CSVs contain PII — keep local only
leads/

hotelfountainbdcrm-493018-a9e9e586dedb.json
.vercel
.env*.local
package.json.bak

.claude-flow/
.swarm/
tsconfig.tsbuildinfo
.mcp.json
.claude/worktrees/
token.txt

# Local-only secrets and one-offs
hotelfountainbdcrm-*.json
.env.production
*.local.json

# stray backup files (sandbox cannot delete)
*.bak
public/*.bak

archive/
supabase/.branches/

# Cowork helper push scripts (local tooling, not source)
scripts/RUN_PUSH_*.bat

# Secret-bearing files — never commit to repo (C1 audit 2026-06-18)
# Rotate FB token + staff passwords before running git filter-repo purge
*.bat
*payload*.json
crm_live.html
static-deploy/crm_live.html
