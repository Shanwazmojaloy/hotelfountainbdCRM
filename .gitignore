node_modules/
.next/
.env*
*.log
# Lead CSVs contain PII — keep local only
leads/

hotelfountainbdcrm-493018-a9e9e586dedb.json
.vercel
.env*.local
package.json.bak

.claude-flow/
.swarm/
tsconfig.tsbuildinfo
.mcp.json
.claude/worktrees/
token.txt

# Local-only secrets and one-offs
hotelfountainbdcrm-*.json
.env.production
*.local.json

# stray backup files (sandbox cannot delete)
*.bak
public/*.bak

archive/
supabase/.branches/

# Cowork helper push scripts (local tooling, not source)
scripts/RUN_PUSH_*.bat

# Local scratch: git patches, push helpers, skill dumps (never source; keep out
# of accidental commits — build audit 2026-07-19)
*.patch
PUSH_*.ps1
agency-master-skill.txt

# Secret-bearing files — never commit to repo (C1 audit 2026-06-18)
# Rotate FB token + staff passwords before running git filter-repo purge
*.bat
*payload*.json
crm_live.html
static-deploy/crm_live.html
/.swc

# Local scratch: git lock files moved aside by the Cowork device bridge
# (device_bash cannot delete, only mv). Never commit these.
_to_delete/

# growth-os site + assets: NOT deployed from this repo (see growth_os_site_source_CORRECTION_2026_08_18)
# real source lives in the separate growth-os-site Vercel project
docs/growth-os/
