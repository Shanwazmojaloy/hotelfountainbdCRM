#!/usr/bin/env bash
# Hotel Fountain — pre-commit hook
# Enables: scripts/guard-onedrive-truncation.sh
# Install: git config core.hooksPath .githooks
set -e
exec bash "$(git rev-parse --show-toplevel)/scripts/guard-onedrive-truncation.sh"
